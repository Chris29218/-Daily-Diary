<%- include('partials/header') -%>

    <div>     
        <!-- Using FOREACH Loop -->
        <%  homeContent.forEach(function(item) { %>
            <h1><%= item.title %></h1>
            <p> <%= item.description.substring(0, 100) + "... " %><a href="/posts/<%=item._id%>">Read More</a></p>
       <%  }); %>
    </div>

<%- include('partials/footer') -%>
